чем package main отличается от обычного пакета;
Package main это точка входа, то есть это место с которого начинается выполнение кода, а всё остальное это просто пакеты
зачем возвращать error;
Для того чтобы основной код панимал, что что-то пошло не так и можно было бы выбрать другой вариант развития событий
почему нельзя игнорировать ошибку записи;
потому что, если не проверить запись, можно потерять все данные работы кода
почему JSON с неэкспортируемыми полями пустой;
потому что encoding/json видет только большие быквы
где должна жить логика хранения.
в папке хранилище(storage)